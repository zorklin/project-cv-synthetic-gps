# Validation

This package contains:

- sensor-level static-pressure diagnostics;
- cold-start temporal replay;
- leave-one-temporal-block-out confidence-policy selection;
- machine-readable CSV and JSON summaries.

Dense GPS may be used as an offline diagnostic score. It is not used as an
online fusion factor or as a confidence-policy input.
