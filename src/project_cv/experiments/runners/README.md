# Experiment runners

These runners load the hash-locked `gtsam-fusion` notebook cell, apply minimal
controlled AST transformations, and always write to a new output directory.
They reproduce research runs but are not the recommended final entry point.
